[pytest]
markers =
    test: Тесты Вебсервер
addopts = -n 2 --capture=no  
log_cli = true
log_cli_level = INFO

log_file = test_logs.log
log_file_level = INFO
log_file_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S